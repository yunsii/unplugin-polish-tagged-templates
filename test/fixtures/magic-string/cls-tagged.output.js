const a = ""

const b = ""

const c = "relative"

const d = "relative ${foo} ${ bar }"

const e = cls`relative
  ${foo}
  ${bar}
  relative
`

const f = cls`${item.value} size-4 shrink-0 md:h-5 md:w-6`
